<%- include('./partials/header') %>
    <div class="container bg-dark text-white messageBox">
        <% msg.forEach(msg=> {%>
            <div class="row text-success">
                <div class="col-sm">
                    <%= msg.name %>
                </div>
                <div class="col-sm-8">
                    <div class="alert alert-dark">
                        <%= msg.text %>
                    </div>
                </div>
                <div class="col-sm">
                    <%= msg.sent/* .toDateString() */ %>
                </div>
            </div>
            <% }); %>

    </div>

    <nav class="navbar fixed-bottom navbar-dark bg-dark">
        <form class="form-inline stopForm">
            <input class="form-control mr-sm-2 bg-dark text-white text" type="search" placeholder="Type...."
                aria-label="Search">
            <button class="btn btn-success my-2 my-sm-0 send" type="submit">SEND</button>
        </form>
    </nav>

    <script src="js/ajax.js"></script>
    <%- include('./partials/footer') %>